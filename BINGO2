<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ビンゴゲーム</title>
<style>
  body {
    font-family: sans-serif;
    text-align: center;
    background: linear-gradient(135deg, #a8edea, #fed6e3);
    margin: 0;
    padding: 20px;
  }

  h1 { margin-bottom: 10px; }

  #board {
    display: grid;
    grid-template-columns: repeat(5, 60px);
    gap: 10px;
    justify-content: center;
    margin: 20px auto;
  }

  .cell {
    width: 60px;
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: white;
    border-radius: 10px;
    font-weight: bold;
    box-shadow: 0 4px 10px rgba(0,0,0,0.1);
  }

  .marked {
    background: #ff7eb3;
    color: white;
  }

  button {
    padding: 10px 20px;
    border: none;
    border-radius: 10px;
    background: #7aa7ff;
    color: white;
    font-size: 16px;
    cursor: pointer;
    margin: 5px;
  }

  #drawn, #history {
    margin-top: 10px;
    font-size: 18px;
  }

  #message {
    margin-top: 20px;
    font-size: 20px;
    font-weight: bold;
  }

  #history {
    max-width: 300px;
    margin: 10px auto;
    background: white;
    padding: 10px;
    border-radius: 10px;
    box-shadow: 0 4px 10px rgba(0,0,0,0.1);
  }
</style>
</head>
<body>

<h1>ビンゴゲーム 🎉</h1>
<button onclick="generateBoard()">リセット</button>
<button onclick="drawNumber()">数字を引く</button>
<button onclick="toggleBGM()">BGM ON/OFF</button>

<div id="drawn">引いた数字：なし</div>
<div id="history">履歴：なし</div>
<div id="board"></div>
<div id="message"></div>

<!-- BGM -->
<audio id="bgm" loop>
  <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
</audio>

<script>
let numbers = [];
let drawnNumbers = [];
let bgmPlaying = false;

function generateBoard() {
  const boardDiv = document.getElementById('board');
  const message = document.getElementById('message');
  const drawn = document.getElementById('drawn');
  const history = document.getElementById('history');

  boardDiv.innerHTML = '';
  message.textContent = '';
  drawn.textContent = '引いた数字：なし';
  history.textContent = '履歴：なし';

  numbers = Array.from({length: 25}, (_, i) => i + 1);
  numbers.sort(() => Math.random() - 0.5);
  drawnNumbers = [];

  numbers.forEach(num => {
    const cell = document.createElement('div');
    cell.classList.add('cell');
    cell.textContent = num;
    boardDiv.appendChild(cell);
  });
}

function drawNumber() {
  if (drawnNumbers.length === 25) return;

  let remaining = numbers.filter(n => !drawnNumbers.includes(n));
  let num = remaining[Math.floor(Math.random() * remaining.length)];

  drawnNumbers.push(num);

  document.getElementById('drawn').textContent = '引いた数字：' + num;
  document.getElementById('history').textContent = '履歴：' + drawnNumbers.join(', ');

  document.querySelectorAll('.cell').forEach(cell => {
    if (Number(cell.textContent) === num) {
      cell.classList.add('marked');
    }
  });

  checkBingo();
}

function checkBingo() {
  const cells = document.querySelectorAll('.cell');
  const grid = [];

  for (let i = 0; i < 5; i++) {
    grid[i] = [];
    for (let j = 0; j < 5; j++) {
      grid[i][j] = cells[i * 5 + j].classList.contains('marked');
    }
  }

  let bingo = false;

  for (let i = 0; i < 5; i++) {
    if (grid[i].every(v => v)) bingo = true;
  }

  for (let j = 0; j < 5; j++) {
    if (grid.every(row => row[j])) bingo = true;
  }

  if ([0,1,2,3,4].every(i => grid[i][i])) bingo = true;
  if ([0,1,2,3,4].every(i => grid[i][4-i])) bingo = true;

  if (bingo) {
    document.getElementById('message').textContent = '🎉 ビンゴ！おめでとう！';
  }
}

function toggleBGM() {
  const bgm = document.getElementById('bgm');
  if (!bgmPlaying) {
    bgm.play();
    bgmPlaying = true;
  } else {
    bgm.pause();
    bgmPlaying = false;
  }
}

// 初期化
generateBoard();
</script>

</body>
</html>

